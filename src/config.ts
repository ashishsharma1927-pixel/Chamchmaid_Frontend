import { Platform } from 'react-native';

export const API_URL = 'https://chamchmaid.onrender.com';
// Use your local IP (e.g. http://192.168.1.8:8001) for physical devices if testing locally

export const WS_URL = API_URL.replace('http', 'ws');
